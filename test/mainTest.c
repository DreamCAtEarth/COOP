#include <coop/coop.h>
#include <util/util.h>

int main(int argc, char **argv)
{
    ENV_(argc, argv);
    newǁ(YPnOLrQT, object1, tab, [2][5][3], (ZOF(*tabArray), ZOF(**tabArray)));
    new(KPLGWrfu, object2, o2, 3, 4.4f, 'p', 5.7);
    vlDzoKUd.Object->setProtectedAttribute(&(*object1)[1][1][0].Derived.Object, "salut la compagnie 3 !");
    showArrayInfos(&tab);
    printf("attribut protégé : %s\n", vlDzoKUd.Object->getProtectedAttribute(findMetadata(object1), &(*object1)[1][1][0].Derived.Object));
    printf("Nom de la classe : %s\n", vlDzoKUd.Object->getClassName());

    kdzvSGAN.Object->setAlternativePrivateAttribute(object2, 8.8);
    printf("attribut privé alternatif : %f\n", kdzvSGAN.Object->getAlternativePrivateAttribute(object2));
    ((
        Ͼ object2 === object1 Ξ Ø
    ));
    printf("Nom de l'attribut : %c\n", kdzvSGAN.Object->getAlternativeProtectedAttribute(object2));
    return 0;
}
